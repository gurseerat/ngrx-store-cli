{
  "name": "ngrx-store-cli",
  "version": "1.0.1",
  "description": "A schematic to generate NgRx store slices",
  "keywords": [
    "schematics",
    "ngrx",
    "angular",
    "store"
  ],
  "license": "MIT",
  "author": "Gurseerat Kaur",
  "type": "module",
  "schematics": "./collection.json",
  "files": [
    "dist/",
    "collection.json",
    "schema.json",
    "README.md"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc && npm run copy:templates",
    "copy:templates": "node copy-template.js"
  },
  "dependencies": {
    "@angular-devkit/schematics": "^20.0.5",
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@angular-devkit/schematics-cli": "^20.0.5"
  }
}
